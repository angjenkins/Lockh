release.status=integration
publish.branch=d
featureset.rev=13
milestone.rev=02
patch.rev=0
release.tag=${featureset.rev}.${milestone.rev}.${patch.rev}
svn.release.tag=d${release.tag}
latest.milestone=${publish.branch}${featureset.rev}.+
latest.release=${publish.branch}${featureset.rev}.${milestone.rev}.+
eas.util.rev=${latest.milestone}
eas.common.rev=${latest.milestone}
client.framework.rev=${latest.milestone}
domain.models.rev=${latest.milestone}
persistence.rev=${latest.milestone}
transformation.rev=${latest.milestone}
etl.services.rev=${latest.milestone}
business.services.rev=${latest.milestone}
rules.framework.rev=${latest.milestone}
bpms.rev=${latest.milestone}
service.manager.rev=${latest.milestone}
cms.rev=${latest.milestone}